version: 1
applications:
  - appRoot: .
    platform: WEBSERVER
    frontend:
      phases:
        build:
          commands:
            - docker build -t connie-one-prod .
      artifacts:
        baseDirectory: /
        files:
          - "**/*"
