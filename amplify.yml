version: 1
applications:
  - appRoot: .
    platform: WEBSERVER
    # For the WEBSERVER platform, Amplify automatically detects your Dockerfile
    # and builds/deploys the container. You don't need to specify any build
    # commands or artifacts here if your Dockerfile handles everything.
